#include <bits/stdc++.h>
using namespace std;

int main() {
    //sorting
    // sort(a,a+n); where a is position
    vector<int> v = {1,5,2,1,9,8,7,6};
    sort(v.begin(),v.end());
    
    //to sort in descending order
    // sort(a, a+n,greater<int>)
    return 0;
}