#include <bits/stdc++.h>
using namespace std;

int main() {
    
    unordered_set<int> us ; //unique only
    //lower and upper bound functions wont work rest are same
    // better tsc than sets in most of the cases except collission
    return 0;
}